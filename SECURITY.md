# Security Policy

CareerPilot 会接触邮箱授权、简历、附件和求职记录。安全问题应优先减少暴露，不要为了复现而上传真实用户数据或凭证。

## 支持范围

| 版本/分支 | 支持状态 |
|---|---|
| `main` 与当前发布候选 | 接受安全修复 |
| 历史提交、个人分支和未维护 Fork | 不保证支持 |

当前版本是本地单用户应用，不是可直接暴露到公网的多租户 SaaS。Stage 8 之前不要把 API 绑定到不可信网络，也不要移除本机来源限制。

## 私下报告漏洞

请使用 GitHub 仓库 **Security → Report a vulnerability** 创建私密安全公告草稿。报告应包含：

- 受影响版本或提交；
- 前置条件和最小复现步骤；
- 可能泄露或被修改的数据；
- 是否涉及真实凭证（不要在报告正文粘贴凭证）；
- 建议修复或缓解措施（如有）。

不要为验证问题访问他人邮箱、账户或求职数据，不要在公开 Issue、PR、日志或截图中暴露 Secret。维护者确认后会协调修复、回归、公告和发布。

## 安全设计边界

- Windows Secret 使用 Credential Manager；Docker 使用只读环境变量或 Secret 文件。
- API 只返回凭证是否存在，不返回凭证值。
- 邮箱访问只读；163 使用 `BODY.PEEK[]`，Gmail/Outlook 使用最小只读 scope。
- OAuth 使用 Authorization Code + PKCE；state 短时、一次性；令牌不写入 SQLite、Excel 或前端存储。
- 上传文件经过大小、类型、签名、宏、路径和解压上限检查；原始文件以内容哈希命名。
- 外部网页经过协议、主机和 SSRF 约束；第三方文本永远不作为系统指令执行。
- Agent 只允许内置工具，不能执行任意代码、Shell、SQL 或跨岗位访问；写操作需要人工批准。
- 浏览器扩展只有 `activeTab`、`scripting` 和本地 API 权限；检测 CAPTCHA 后停止，永不提交表单。

## 部署责任

- 仅使用锁定依赖和已扫描镜像；不要在镜像、Compose 文件或命令历史中硬编码 Secret。
- 将 `data/` 与 `secrets/` 权限限制给当前用户，备份也应加密并受同等访问控制。
- 变更 OAuth 权限、允许的 URL、文件类型、Agent 工具或网络监听地址时，必须进行单独安全评审。
- 发现凭证可能泄露时先在供应商处撤销/轮换，再清理日志和受影响副本；仅从 Git 历史删除文本不能使凭证重新安全。

自动化门禁包括 Python/npm 依赖审计、Gitleaks、Trivy、Ruff、后端测试和浏览器 E2E。详细命令见 [交付说明](docs/DELIVERY_P0_P3.md)。
